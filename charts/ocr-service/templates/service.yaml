apiVersion: v1
kind: Service
metadata:
  name: {{ include "ocr-service.fullname" . }}
  labels:
    {{- include "ocr-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "ocr-service.selectorLabels" . | nindent 4 }}
